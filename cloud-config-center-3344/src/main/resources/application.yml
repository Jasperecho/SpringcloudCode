server:
  port: 3344

spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Jasperecho/springcloud-config.git
          username: Jasperecho
          password: Yjj991001
          search-paths:
            - springcloud-config
      label: main

eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka
